import { MaterialComponentComponent } from './material-component.component'

describe('MaterialComponentComponent', () => {
  it('should mount', () => {
    cy.mount(MaterialComponentComponent)
  })
})